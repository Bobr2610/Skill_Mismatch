/**
 * Application configuration — aggregated config
 * DevMetrics / ProdEngine — Employee Evaluation Platform
 */

const APP_CONFIG = {
  app: {
    name: 'DevMetrics',
    brand: 'ProdEngine',
    apiBaseUrl: '/api',
  },
  theme: THEME,
  icons: ICONS,
  routes: ROUTES,
  nav: NAV_ITEMS,
};
