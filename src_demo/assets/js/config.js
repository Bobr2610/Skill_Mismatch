/**
 * Config loader — loads config modules in order
 * Replaced by config/ folder; this file ensures backward compatibility
 */
// Config modules must be loaded before this: theme.js, icons.js, routes.js, index.js
